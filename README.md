# CyberSecurityServer

Consent-based WebSocket signaling starter for a WebRTC camera-sharing application.

Run:
```bash
npm install
npm start
```

Use HTTPS when deployed. Camera permission must be requested visibly by the guest browser.
Sessions expire after 15 minutes.
